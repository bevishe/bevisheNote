# 1.SpringBoot 入门

## 1.SpringBoot简介

主要是为了简化Spring的开发配置环境，是基于Spring框架的一个再封装；j2ee开发的一站式解决方案

![image-20201217130134530](https://bevishe.oss-cn-hangzhou.aliyuncs.com/img/image-20201217130134530.png)

## 2.微服务

![image-20201217131306439](https://bevishe.oss-cn-hangzhou.aliyuncs.com/img/image-20201217131306439.png)

martin Fowler（2014）

微服务：一种架构风格，

一个应用应该是一小型服务；可以通过http的方式进行互通

单体应用：All in one

----

## 3.SpringBoot Helloworld

浏览器发送hello请求，服务器接收请求并返回helloworld

### 3.1 创建maven项目工程

### 3.2 导入依赖

### 3.3 编写主程序，启动Springboot应用

![image-20201217223755473](https://bevishe.oss-cn-hangzhou.aliyuncs.com/img/image-20201217223755473.png)

### 3.4 编写相关的Controller和Service

![image-20201217224004470](https://bevishe.oss-cn-hangzhou.aliyuncs.com/img/image-20201217224004470.png)

### 3.5简化部署

![image-20201217231433816](https://bevishe.oss-cn-hangzhou.aliyuncs.com/img/image-20201217231433816.png)

无需在目标服务器安装tomcat服务器

----

## 4.Pox文件

### 1.parent

```xml
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.1.2.RELEASE</version>
<!--        <relativePath/> &lt;!&ndash; lookup parent from repository &ndash;&gt;-->
    </parent>

他的父项目

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>2.1.2.RELEASE</version>
        <relativePath>../../spring-boot-dependencies</relativePath>
    </parent>
他是真正管理Spring boot项目的所有项目版本
```

SpringBoot项目的版本仲裁中心

以后我们导入依赖默认是不需要写版本号，没有在dependencles里面管理的依赖需要声明版本号

### 2.导入依赖

```xml
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

```

spring-boot-starter-web:

​	spring-boot-starter：spring boot 场景启动器，导入了web模块正常运行的组件。

Spring Boot将所有的功能场景都抽取出来，做成一个个启动器（Starter），只需要在项目里面导入这些starter相关场景的所有依赖导入进来，要用什么功能就导入什么场景的启动器。

## 5.主程序类，主入口类

```java
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
@Documented
@Inherited
@SpringBootConfiguration
@EnableAutoConfiguration
@ComponentScan(excludeFilters = {
		@Filter(type = FilterType.CUSTOM, classes = TypeExcludeFilter.class),
		@Filter(type = FilterType.CUSTOM, classes = AutoConfigurationExcludeFilter.class) })
```



**@SpringBootApplication**

SpringBootConfiguration：的配置类，标注在某一个类上，标识该类为一个配置类，配置类也是容器中的一个组件

**@AutoConfigurationPackage**

​	**@Import(AutoConfigurationPackages.Registrar.class)**

有了自动配置类，免去了自动配置

​	Spring的底层容器注解@import，给容器导入一个组件；导入的组件由AutoConfigurationPackages.Registrar.class；

将主配置类@SpringBootApplication标注的类所在包下面以及下面所有的子包里面的所有组件都扫扫描到Spring容器里面；

​	**@Import(AutoConfigurationImportSelector.class)**:

AutoConfigurationImportSelector：导入哪些组件的选择器；将所有需要导入的组件以全类名的方式返回，这些组件就会被添加到容器中，会给容器导入非常多的自动配置类（xxxAutoConfiguration）;



